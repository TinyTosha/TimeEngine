name: "Example Script"
id: 1
callonstart: true
script:
  # Give

  - "$function.nullstroke"

  - "log.green('Give example item...')"
  - "$inventory.GiveItem(1, 0)"
  
  # Spawn

  - "$function.nullstroke"
  
  - "log.green('Spawn example entity...')"
  - "$enemy.spawn(0, 300, 200, true)"
  - "$enemy.spawn(0, 300, 500, true)"
  - "$enemy.spawn(1, 700, 350, true)"

  - "$function.nullstroke"

  - "$quest.Give(0)"
